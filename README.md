# apps.countryboycreative.com

Static site for Country Boy Creative's apps: an index, and per-app support and privacy pages.
Served by GitHub Pages from `master`. No build step — plain HTML and one stylesheet.

Add an app: copy `circlet/` to `<app>/`, edit the three pages, add a card to `index.html`.
